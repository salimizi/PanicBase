# Build environment variables required for compilation
# Copy to .env.local (never commit this file)

# 32-byte key used to seal the knowledge base at build time (64 hex chars)
# Generate: openssl rand -hex 32
PANICBASE_KB_KEY=your_64_hex_char_key_here
